## 1. Setup & Foundations

- [x] 1.1 Define TypeScript interfaces for `Beverage` and component state.
- [x] 1.2 Initialize the basic component structure in `App.tsx`.

## 2. State & Logic Implementation

- [x] 2.1 Implement `totalCaffeine` state using `useState`.
- [x] 2.2 Create a constant array for the four beverage presets.
- [x] 2.3 Implement the `addCaffeine` function to update state based on beverage selection.
- [x] 2.4 Implement the `resetCaffeine` function to clear the total.

## 3. UI Implementation

- [x] 3.1 Build the header showing "Total Consumed: Xmg / 400mg".
- [x] 3.2 Implement the progress bar component with dynamic width calculation.
- [x] 3.3 Add logic to switch progress bar color to red when total >= 320mg.
- [x] 3.4 Map through beverage presets to render addition buttons.
- [x] 3.5 Render the "Zerar Contagem" button.

## 4. Styling & Refinement

- [x] 4.1 Apply `StyleSheet` to ensure center alignment and proper spacing.
- [x] 4.2 Refine button styles for better touch targets.
- [x] 4.3 Verify responsiveness and layout consistency.
